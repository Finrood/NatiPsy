server {
    listen 8080;
    server_name localhost;
    absolute_redirect off;
    server_tokens off;

    root /usr/share/nginx/html;
    index index.html index.htm;

    # Compression
    gzip on;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/json
        application/xml
        application/xml+rss
        image/svg+xml;

    # The production contract is static prerendered HTML. Known extensionless
    # routes resolve to their generated index.html; unknown routes stay 404.
    location / {
        include /etc/nginx/snippets/natipsy-security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate";

        try_files $uri/index.html $uri @not_found;
    }

    location = / {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate" always;
        try_files /index.html =404;
    }

    # Canonical URLs are extensionless. Keep slash variants from becoming
    # duplicate documents while avoiding Nginx's directory redirect.
    location = /blog/ {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate" always;
        return 301 /blog$is_args$args;
    }

    location ~ ^/blog/([^/]+)/$ {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate" always;
        return 301 /blog/$1$is_args$args;
    }

    # Prerendered directories otherwise serve these slash variants as 200,
    # despite their no-slash canonical tags. Preserve the original query.
    location ~ ^/(terapia-online|orientacao-profissional|contato-e-privacidade|blog/category/[^/]+)/$ {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate" always;
        return 301 /$1$is_args$args;
    }

    # The branded error document is a response body, not a public 200 page.
    location ~ ^/404/?$ {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate" always;
        return 404;
    }

    location @not_found {
        include /etc/nginx/security-headers.conf;
        add_header Cache-Control "no-cache, must-revalidate" always;
        return 404;
    }

    error_page 404 =404 /404/index.html;

    location = /404/index.html {
        internal;
        include /etc/nginx/security-headers.conf;
        add_header X-Robots-Tag "noindex, nofollow" always;
        add_header Cache-Control "no-cache, must-revalidate" always;
    }

    location = /feed.xml {
        include /etc/nginx/security-headers.conf;
        # Override the inherited types map so text/xml cannot win over RSS.
        types { }
        default_type application/rss+xml;
        add_header Cache-Control "public, max-age=300, must-revalidate" always;
        try_files /feed.xml =404;
    }

    # Hashed build artifacts (e.g. main-XMWJPNV4.js, styles-Q7GUAVUT.css):
    # the 8-char content hash in the filename changes on every rebuild,
    # so these are safe to cache immutably for a year.
    # The shared header include is repeated as an include (not copied policy)
    # because Nginx stops inheriting add_header directives once a block
    # declares any of its own. Regex locations match in file order — this block must
    # stay before the generic static-media block below.
    location ~* "-[A-Za-z0-9_-]{8}\.(?:js|css)$" {
        access_log off;
        include /etc/nginx/snippets/natipsy-security-headers.conf;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # Unhashed static media (logo.png, NatiHero.webp, favicon.ico, ...):
    # filenames carry no build hash, so a 1-year immutable cache would pin
    # stale assets for up to a year after an update. Cache for 7 days with
    # ETag/Last-Modified revalidation instead.
    location ~* \.(?:ico|gif|jpe?g|png|webp|svg|woff2?|eot|ttf)$ {
        access_log off;
        include /etc/nginx/snippets/natipsy-security-headers.conf;
        add_header Cache-Control "public, max-age=604800, must-revalidate";
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
        include /etc/nginx/snippets/natipsy-security-headers.conf;
    }
}
